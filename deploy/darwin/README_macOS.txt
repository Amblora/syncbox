SyncBox macOS 安装说明
======================

方式一：终端直接运行（推荐）
1. 解压 client-darwin-arm64.zip
2. 终端进入目录，执行：
   xattr -cr syncbox-client
   chmod +x syncbox-client
   ./syncbox-client

方式二：使用 App Bundle
1. 解压后终端执行：
   xattr -cr SyncBox.app
2. 双击 SyncBox.app 运行

如果提示"无法验证开发者"：
  系统设置 > 隐私与安全性 > 仍要打开
